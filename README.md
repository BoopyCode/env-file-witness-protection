# .env Witness Protection Program

Protects your .env files from accidentally being committed to Git. Extracts sensitive files to a safe location and creates decoy .env.example files. Run: `./env-witness-protection.sh protect` when you need to relocate a witness.